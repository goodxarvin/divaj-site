version: '3.8'

services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dadi_next_site
    restart: always
    ports:
      - "80:3000"
    environment:
      - NODE_ENV=production
